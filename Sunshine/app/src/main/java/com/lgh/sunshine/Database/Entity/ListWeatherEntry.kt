package com.lgh.sunshine.Database.Entity

import java.util.*

class ListWeatherEntry(id : Int, weatherIconId : Int, date : Date, min : Double, max : Double)
{
    var id = id
    var weatherIconId = weatherIconId
    var date = date
    var min = min
    var max = max
}
